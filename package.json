{
  "name": "token-overrides",
  "version": "1.0.0",
  "type": "module",
  "module": "dist/index.js",
  "repository": "git@github.com:ctjlewis/token-overrides.git",
  "author": "Christian Lewis <1657236+ctjlewis@users.noreply.github.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc",
    "write": "node scripts/write.mjs",
    "stage": "git add --all && git stage --all",
    "pre-push": "yarn write && yarn stage && (git commit -m 'chore: built updated overrides JSON' || true)"
  },
  "dependencies": {
    "glob": "^7.1.7"
  },
  "devDependencies": {
    "@types/glob": "^7.1.3",
    "husky": "^6.0.0"
  }
}
